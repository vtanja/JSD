/// Generated from {{ self._TemplateReference__context.name | get_template_name_from_path }} by SBAG (https://github.com/vtanja/JSD)
/// on {{ date }}

import { NgModule } from '@angular/core';
import { Routes, RouterModule } from '@angular/router';
{% set entity_name = entity.name | lower %}
import { {{ entity.name }}Component } from './{{ entity.name | format_file_name}}/{{ entity.name | format_file_name}}.component';
import { {{ entity.name }}ListComponent } from './{{ entity.name | format_file_name}}-list/{{ entity.name | format_file_name }}-list.component';
import { {{ entity.name }}FormComponent } from './{{ entity.name | format_file_name }}-form/{{ entity.name | format_file_name}}-form.component';

const routes: Routes = [
  { path: 'create', component: {{ entity.name }}FormComponent },
  { path: ':id', component: {{ entity.name }}Component },
  { path: ':id/update', component: {{ entity.name }}FormComponent },
  { path: '', component: {{ entity.name }}ListComponent },
];

@NgModule({
  imports: [RouterModule.forChild(routes)],
  exports: [RouterModule]
})

export class {{ entity.name }}RoutingModule { }
