/// Generated from {{ self._TemplateReference__context.name | get_template_name_from_path }} by SBAG (https://github.com/vtanja/JSD)
/// on {{ date }}

import { ComponentFixture, TestBed } from '@angular/core/testing';

import { {{entity.name}}Component } from './{{entity.name|lower}}.component';

describe('{{entity.name}}Component', () => {
  let component: {{entity.name}}Component;
  let fixture: ComponentFixture<{{entity.name}}Component>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ {{entity.name}}Component ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent({{entity.name}}Component);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
