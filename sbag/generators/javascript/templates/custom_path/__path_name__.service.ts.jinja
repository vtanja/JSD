/// Generated from {{ self._TemplateReference__context.name | get_template_name_from_path }} by SBAG (https://github.com/vtanja/JSD)
/// on {{ date }}

import { HttpClient } from '@angular/common/http';
import { Injectable } from '@angular/core';
import { environment } from 'src/environments/environment';

{% set path = path_name | capitalize_first_letter %}
{% set service_name = path + 'Service' %}
{% set path_lower = path_name | lower %}



@Injectable({
  providedIn: 'root'
})
export class {{service_name}} {
 
    constructor(private http: HttpClient) { }

    {% for endpoint in new_controllers[path_lower] %}
    {% set comma_separator = joiner() %}

    {{ endpoint.function_name() }}({% for param in endpoint.parameters %}{{ comma_separator() }}String {{ param }}{% endfor %}{% if endpoint.post_type is not none %}{% if endpoint.parameters | length > 0 %}, {% endif %}requestObject: I{{ endpoint.post_type }} {% endif %}){
    {%if endpoint.function_name() == 'get' %}
        return this.http.{{ endpoint.function_name() }}<{{ endpoint | get_correct_type_custom_paths }}>(environment.api + '/{{ path_lower }}/{{ endpoint.path }}');
    {%elif  endpoint.function_name() == 'post'%}
        //post
    {%elif  endpoint.function_name() == 'put'%}
        //put
    {%elif  endpoint.function_name() == 'delete'%}
        //delete
    {%endif%}
    }

    {% endfor %}

}